# Password-Generator
A command-line password generator built with Python that creates secure random passwords using customizable length input.

## Features
- Minimum length: 8
- Includes uppercase, lowercase, digits, and symbols
- Uses Python's 'secrets' module for secure randomness

## How to Run
python password_generator.py
